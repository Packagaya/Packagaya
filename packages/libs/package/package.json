{
    "name": "@packagaya/package",
    "license": "MIT",
    "version": "1.1.4",
    "scripts": {
        "prebuild": "npm run clean",
        "build": "tsc --build",
        "prebuild:watch": "npm run clean",
        "build:watch": "tsc --build --watch",
        "clean": "rimraf dist tsconfig.tsbuildinfo",
        "test": "jest",
        "test:watch": "jest --watch"
    },
    "dependencies": {
        "@packagaya/config": "*",
        "inversify": "^5.0.5"
    },
    "devDependencies": {
        "@types/node": "^14.14.25",
        "jest": "^26.6.3",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.5.1",
        "typescript": "^4.1.5"
    },
    "publishConfig": {
        "access": "public"
    }
}

// Defines the IoC service names
export const Services = {
    // The schema object
    Schema: {
        // The symbol for the schema configuration
        Config: Symbol.for('Schema.Config'),
    },
};

{
    "name": "@packagaya/cli",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "clean": "rimraf dist",
        "prebuild": "npm run clean",
        "build": "tsc --build",
        "prebuild:watch": "npm run clean",
        "build:watch": "tsc --build --watch",
        "start": "ts-node-dev --respawn --no-notify --clear ./src/main.ts",
        "test": "jest",
        "test:watch": "jest --watch"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^14.11.2",
        "jest": "^26.4.2",
        "rimraf": "^3.0.2",
        "ts-jest": "^26.4.1",
        "ts-node-dev": "^1.0.0-pre.63",
        "typescript": "^4.0.3"
    }
}
